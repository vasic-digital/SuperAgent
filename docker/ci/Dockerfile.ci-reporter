# HelixAgent CI - Report Aggregator
FROM node:20-alpine

RUN apk add --no-cache git bash jq

WORKDIR /opt/reporter

COPY ci/reporter/package.json ci/reporter/aggregate.js ci/reporter/dashboard-template.html ./
RUN npm install --production

ENV WORKSPACE=/workspace

ENTRYPOINT ["node", "/opt/reporter/aggregate.js"]
