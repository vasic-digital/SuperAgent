# MCP Redis Server - Redis integration
FROM node:20-alpine

WORKDIR /app

COPY . .

RUN npm install && npm run build 2>/dev/null || true

ENV REDIS_URL="redis://localhost:6379"

EXPOSE 3000

CMD ["node", "dist/index.js"]
