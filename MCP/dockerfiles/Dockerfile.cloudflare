# MCP Cloudflare Server - Workers, KV, R2, D1
FROM node:20-alpine

WORKDIR /app

COPY . .

RUN npm install && npm run build 2>/dev/null || true

ENV CLOUDFLARE_API_TOKEN=""
ENV CLOUDFLARE_ACCOUNT_ID=""

EXPOSE 3000

CMD ["node", "dist/index.js"]
