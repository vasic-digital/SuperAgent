# MCP MongoDB Server - MongoDB integration
FROM node:20-alpine

WORKDIR /app

COPY . .

RUN npm install && npm run build 2>/dev/null || true

ENV MONGODB_URI="mongodb://localhost:27017/helixagent"

EXPOSE 3000

CMD ["node", "dist/index.js"]
