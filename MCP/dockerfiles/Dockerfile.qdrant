# MCP Qdrant Server - Vector search
FROM python:3.11-slim

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -e . 2>/dev/null || pip install --no-cache-dir mcp-server-qdrant

ENV QDRANT_URL="http://localhost:6333"

EXPOSE 3000

CMD ["python", "-m", "mcp_server_qdrant"]
