# MCP Memory Server - Knowledge graph persistent memory
FROM node:20-alpine

WORKDIR /app

# Copy from MCP-Servers submodule
COPY src/memory ./

# Install dependencies and build
RUN npm install && npm run build 2>/dev/null || true

VOLUME ["/data"]

EXPOSE 3000

CMD ["node", "dist/index.js"]
