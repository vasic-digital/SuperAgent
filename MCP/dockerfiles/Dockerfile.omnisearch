# MCP Omnisearch Server - Unified search
FROM node:20-alpine

WORKDIR /app

COPY . .

RUN npm install && npm run build 2>/dev/null || true

ENV TAVILY_API_KEY=""
ENV BRAVE_API_KEY=""
ENV PERPLEXITY_API_KEY=""

EXPOSE 3000

CMD ["node", "dist/index.js"]
