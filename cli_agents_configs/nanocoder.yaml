# SPDX-FileCopyrightText: 2026 Milos Vasic
# SPDX-License-Identifier: Apache-2.0
#
# HelixAgent Exported Configuration: Nanocoder
# Category: Coding
# Tier: 3

version: "1.0"
agent_type: "nanocoder"
name: "Nanocoder"
description: "HelixAgent integration configuration for Nanocoder"

helixagent:
  endpoint: "http://localhost:7061"
  api_key: "${HELIXAGENT_API_KEY:-default}"
  primary_model:
    provider: "ensemble"
    model: "helixagent/ensemble"
    temperature: 0.7
    max_tokens: 4096
  fallback:
    enabled: true
    providers: ["claude", "deepseek", "gemini", "openrouter"]
    strategy: "sequential"
  mcp:
    enabled: true
    server_url: "http://localhost:7061/v1/mcp"
    tools:
      - read_file
      - write_file
      - edit_file
      - bash
      - search
      - git
    auto_approve_tools:
      - read_file
      - search
  debate:
    enabled: true
    default_topology: "mesh"
    min_agents: 2
    max_agents: 5
    auto_debate_threshold: 0.7
  # Basic features for Tier 3 agents
  semantic_search:
    enabled: false
  context_templates:
    enabled: false

logging:
  level: "info"
  format: "json"
  output: "stdout"
